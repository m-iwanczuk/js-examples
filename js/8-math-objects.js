/* Właściwości obiektu Math */

const pi = Math.PI;
console.log(pi);

const eulersConst = Math.E;
console.log(eulersConst);

/* Metody obiektu Math */

let square = Math.pow(2, 16); // (podstawa, wykładnik)
console.log(square);

let randomNumber = Math.random();
console.log(randomNumber);